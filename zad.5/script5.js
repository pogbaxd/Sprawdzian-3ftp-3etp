let firstNumber = Number(prompt('wpisz pierwszą liczbę: '));
let secondNumber = Number(prompt('wpisz drugą liczbę: '));
let thirdNumber = Number(prompt('wpisz trzecisz liczbę: '));

document.write(`Największa liczba to : ${Math.max(firstNumber, secondNumber, thirdNumber)}`);